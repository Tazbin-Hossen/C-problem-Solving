#include<bits/stdc++.h>
#define ll long long
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
using namespace std;
int main(){
    optimize();
    string name="mahadi";
    if(name.find("aha")!=-1){cout<<"found";}
    else{cout<<"not found";}

    return 0;
}