#include<bits/stdc++.h>
#define ll long long
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
using namespace std;
int main(){
    optimize();
    ll n; 
    while(cin>>n)
    {
        if(n==1999){cout<<"Correct"<<endl;break;}
        else{cout<<"Wrong"<<endl;}
    }
    return 0;
}